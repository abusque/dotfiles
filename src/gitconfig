[user]
        name = Antoine Busque
        email = antoine.busque@pillrcompany.com
        signingkey = 7E261FE9
[color]
        ui = auto
        ui = true
[push]
        default = current
[alias]
        br = branch
        ci = commit -s
        cim = commit -sm
        cia = commit --amend --no-edit
        cam = commit -sam
        co = checkout
        cp = cherry-pick
        rb = rebase
        rbi = rebase -i
        st = status
        lppg = log --graph --pretty=format:'%Cred%h%Creset %an: %s - %Creset %C(yellow)%d%Creset %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
        discard = stash --keep-index -u
        k = !gitk
        d = diff
        ds = diff --staged
        changelog = log --pretty='%x09* %s'
[sendemail]
        smtpencryption = tls
        smtpserver = smtp.gmail.com
        smtpuser = antoinebusque@gmail.com
        smtpserverport = 587
        suppresscc = self
        annotate = true
[merge]
        conflictstyle = diff3
[core]
        excludesfile = ~/.gitignore_global
[log]
        follow = true
[rebase]
	autosquash = true
