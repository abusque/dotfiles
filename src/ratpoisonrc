set startupmessage 0
set bargravity se

set padding 0 20 0 0
set font "Terminus-12"

exec /usr/bin/rpws init 4 -k

bind c exec urxvt
bind e exec emacs
bind f exec chromium
bind o sselect 0
bind p sselect 1
bind a exec slock
bind l focusright
bind h focusleft
bind m exec togglexkbmap
bind x exec keepassxc
bind d exec tomate
bind y exec shh
